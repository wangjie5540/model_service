{
  "sample_select": {
    "active_before_days": ${sample_select__active_before_days},
    "active_after_days": ${sample_select__active_after_days},
    "start_date": "${sample_select__start_date}",
    "end_date": "${sample_select__end_date}",
  },
  "model": {
    "max_depth": ${model__max_depth},
    "scale_pos_weight": ${model__scale_pos_weight},
    "n_estimators": ${model__n_estimators},
    "learning_rate": ${model__learning_rate},
    "train_table_name": "${model__train_table_name}",
    "test_table_name": "${model__test_table_name}",
  },
  "feature_create": {
    "active_before_days": ${feature_create__active_before_days},
    "active_after_days": ${feature_create__active_after_days},
    "start_date": "${feature_create__start_date}",
    "end_date": "${feature_create__end_date}",
    "mid_date": "${feature_create__mid_date}",
  },
  "feature_create_predict": {
    "active_before_days": ${feature_create_predict__active_before_days},
    "active_after_days": ${feature_create_predict__active_after_days},
    "start_date": "${feature_create_predict__start_date}",
    "end_date": "${feature_create_predict__end_date}",
    "sample": "${feature_create_predict__sample}",
  }
  "model-predict": {
    "model_hdfs_path": "${model_predict__model_hdfs_path}",
  },
}